apiVersion: v1
kind: Service
metadata:
  name: website-svc
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: website-deployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80